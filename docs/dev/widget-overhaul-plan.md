# 桌面小组件重构方案

状态：产品规格 v2（2026-09-12）

## 产品模型

小组件分为三层：基础模板、我的小组件设计、桌面实例。基础模板只提供布局，不直接添加到桌面；用户先创建并保存设计，再从“我的小组件”添加。一个设计可以绑定多个桌面实例，编辑设计后所有实例同步更新。

设计定义是可迁移的用户数据，桌面 `appWidgetId` 只是设备本地绑定，不进入备份。删除仍被桌面使用的设计时，先提示解绑，不能留下空白组件。

## 尺寸与模板

支持 1×2、2×2、2×4 三种预览尺寸，名称按「行 × 列」解释：1×2 横向、2×2 正方形、2×4 横向宽卡。旧版试验尺寸读取时映射到这三类，编辑页不再提供旧尺寸。Android 启动器可调整实际尺寸。

- 1×2：主指标和一个快速记账入口或辅助值。
- 2×2：主指标加两个辅助指标，或主指标加预算进度环。
- 2×4：左侧主指标和辅助指标，右侧图表或预算进度。

首批基础模板：快速记账、预算进度、收支趋势、净资产。每个尺寸限制可用插槽数量，避免小组件被文字和图表挤满。

## 设计配置

每个用户设计包含：稳定 UUID、名称、模板、默认尺寸、主指标、按顺序排列的辅助指标、图表类型/序列/日期范围/粒度、账本范围、账户/分类/标签筛选、点击动作、金额隐私显示、背景配置和更新时间。

指标首批覆盖今日/周期收支、预算剩余与使用率、结余、净资产、资产/负债、账户余额、交易笔数和储蓄率。图表只表达一个问题：收支趋势、预算消耗、净资产变化或账户余额变化。

## 背景与视觉

背景支持内置主题、纯色和本地相册图片。用户图片支持裁切焦点和明暗遮罩；图片作为不透明表面使用，不引入玻璃、模糊或折射效果。文字、图表和状态必须满足对比度要求，图片失败时回退到纯色主题。

组件遵循“大数字 + 小指标 + 一个视觉重点”的层级。缺率、超支、过期和隐藏金额必须有文字状态，不能只靠颜色表达。

## 用户流程

“我的 → 桌面小组件”就是“我的小组件”画布，只展示用户已经保存的设计；基础模板不出现在这个页面。右上角加号进入独立的创建页，创建完成后返回画布。

画布按组件尺寸使用自适应瀑布流布局：窄屏两列，宽组件跨两列，删除或移动后立即重新计算位置。正常状态不在组件外层包裹管理卡片或按钮；组件本身就是预览。普通点击进入编辑页，长按进入编辑模式，在组件右上角显示删除入口和拖动反馈。

编辑态交互细节：长按组件即进入拖动状态，画布中的组件短暂持续抖动；删除入口只属于长按的那一个组件，显示在其右上角外侧并使用错误色圆形按钮。画布空白点击退出编辑态；拖动反馈保持被拖组件的实际尺寸，空白网格也能作为插入目标。

画布不显示“已保存的小组件”副标题或独立编辑按钮。长按后按返回退出管理模式；拖动排序持久化。创建页预览、画布和拖动反馈使用同一有界绘制组件，2×2 宽高必须相等，名称与金额共享左边距；记账按钮放在内容区右侧/右下方且不与文字重叠。

新建流程：选择模板和尺寸 → 选择账本 → 填充主/辅助指标 → 配置图表范围与筛选 → 选择背景与点击动作 → 预览 → 保存到“我的小组件”。

点击已保存组件进入编辑页，编辑页提供保存、删除和添加到桌面。长按删除需要确认；删除后画布自动重排。Android 配置 Activity 也只展示已保存设计，并把选中的设计 ID 与新 `appWidgetId` 绑定。

## 导出与导入

备份 JSON v3 增加 `userWidgets`。定义中保存配置和背景资源引用，不保存 `appWidgetId`。内置背景保存主题 ID；本地图片复制为应用资源并随主动备份导出，导入时重新生成本地资源 ID。

导入先校验枚举、尺寸、插槽数量及账本/账户/分类/标签引用；无效引用清理或回退到当前账本和空筛选，不影响账目导入。旧版备份没有 `userWidgets` 时按空列表处理。恢复后设计保留在“我的小组件”，用户重新选择要添加的桌面实例。

清除本地数据时同时清除设计和设备绑定；设计配置不包含密钥、口令、WebDAV 凭证、AI 凭证或聊天历史。

## 刷新与点击

2026-09-12 当前实现校正：用户设计经 `buildWidgetPresentation` 生成同源的 Flutter 预览与 Android 数值快照，按设计账本与日期范围取数，不再回退到当前账本的全局支出值。保存设计后先同步原生快照，再请求系统固定；固定回调用显式可变 PendingIntent 接收启动器填入的 `appWidgetId`。RemoteViews 遮罩使用可支持的 ImageView，记账按钮有独立点击区域。冷启动由 Shell 消费待处理路由并切换到设计账本。系统格子的实际宽高由启动器决定；应用内 2×2 预览固定正方形。

以下仍是完整方向：用户设计跨日自动重算、图表点击携带全部筛选、记账预填账户和分类，尚不能以已实现验收。

交易、退款、预算、汇率、账户和账本变化后刷新相关设计的所有实例；跨日和跨预算周期重新计算，失败时保留可解释的旧数据并显示刷新状态。

整卡点击使用设计主动作；图表区域进入带相同账本、日期范围和筛选的统计页；快速记账按钮预填设计指定的账本、类型、账户和分类。

## 开发阶段

1. 建立 `UserWidgetDefinition`、背景资源和 `WidgetPlacement` 模型，完成 v3 备份往返与旧配置迁移。
2. 重做“我的小组件”管理页、创建/编辑/复制/删除和多尺寸预览。
3. 增加统一 Android `UserWidgetProvider`、配置 Activity、尺寸布局、背景 bitmap 与点击路由。
4. 把设计 ID 绑定到桌面实例，实现编辑同步、删除解绑和刷新调度。
5. 兼容现有四个 Provider，完成真机/模拟器、浅深色、大字体、不同启动器和导入恢复验收。

本方案不改变交易、预算、退款和多币种计算口径；小组件只读取这些领域已有的权威结果。

## 2026-09-12 回归验收

- Flutter 3.47.2，正式 `lib/main.dart`，隔离 `diagnostic` flavor，显式 `UNIFIED_DESIGN_PREVIEW=true`；未清除或覆盖正式应用数据。
- `dart format .`、`flutter analyze --no-pub` 通过；`flutter test --no-pub` 共 1059 项通过、5 项按现有配置跳过。
- 自动回归覆盖所有模板 × 三种尺寸、深浅主题、1.5 倍文字缩放、真实金额、四角裁切、文字左对齐及记账按钮边界；覆盖创建、编辑、选择器取消、长按删除、拖动持久化、跨账本原生快照和冷启动路由。
- API 36 模拟器逐项操作：空画布 → 加号 → 创建 → 2×2 保存 → 点击编辑 → 背景菜单 → 系统桌面添加 → 点击记账；结束后台应用进程后，从桌面再次点击能冷启动打开记账。
- 模拟器另行验证 1×2、2×4、日期范围切换、真实年度支出 808、取消删除、确认删除、拖动排序及删除后补位。创建与冷启动后设计均保留，当前进程未出现 Flutter 异常或布局溢出，Android crash buffer 无新增记录。
- 本地截图与日志保留在被忽略的 `build/widget-*.png` / `build/widget-*.txt`，避免提交测试账本内容。正式发布仍需要授权后的 CI 构建与手机验收。
