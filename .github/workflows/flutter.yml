name: Flutter CI

on:
  push:
    tags:
      - 'v*.*.*'

permissions:
  contents: write

jobs:
  checks:
    name: Analyze, Test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Install dependencies
        run: flutter pub get

      - name: Analyze
        run: flutter analyze

      - name: Test
        run: flutter test

  android:
    name: Android APK + AAB
    runs-on: ubuntu-latest
    needs: checks
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Install dependencies
        run: flutter pub get

      - name: Build release APK
        # 只构建 arm64-v8a：2019 年后的安卓机几乎全是 arm64，一个包覆盖 99%+ 设备，
        # 相比 universal（打包 armeabi-v7a/arm64/x86_64 三套原生库）体积约减半。
        # 极老的 32 位机型装不了——已在 Release 说明中告知。
        run: flutter build apk --release --target-platform android-arm64

      - name: Build release App Bundle
        # Google Play 上架 / 所有权验证需要 AAB（.aab）。与 APK 同用 verifinRelease
        # 签名配置签名；AAB 内含全部 ABI，由 Play 按设备分发（自建 APK 分发仍走上面的
        # arm64 APK）。
        run: flutter build appbundle --release

      - name: Prepare release artifacts
        shell: bash
        run: |
          mkdir -p release-artifacts
          short_sha="${GITHUB_SHA::7}"
          version_name="${GITHUB_REF_NAME}"
          cp build/app/outputs/flutter-apk/app-release.apk "release-artifacts/verifin-${version_name}-arm64-${short_sha}.apk"
          cp build/app/outputs/bundle/release/app-release.aab "release-artifacts/verifin-${version_name}-${short_sha}.aab"

      - name: Upload release artifacts
        uses: actions/upload-artifact@v4
        with:
          name: verifin-android-release
          path: release-artifacts/*

  release:
    name: GitHub Release
    runs-on: ubuntu-latest
    needs: android
    steps:
      - name: Download release artifacts
        uses: actions/download-artifact@v4
        with:
          name: verifin-android-release
          path: release-artifacts

      - name: Create pre-release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            release-artifacts/**/*.apk
            release-artifacts/**/*.aab
          # 默认发预发布：不标记为 Latest，待真机测试通过后在 GitHub 手动改为正式版。
          prerelease: true
          make_latest: 'false'
          body: |
            ⚠️ 预发布（待验证）：此版本由 CI 自动构建，尚未经真机验收。

            📦 安装包为 **arm64-v8a** 架构，覆盖 2019 年后绝大多数安卓机型。极少数 32 位老设备可能无法安装。

            💡 本版含存储与性能优化，更新前建议先在「数据管理」里**导出一次备份**以防万一。
