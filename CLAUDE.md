# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

Veri Fin (包名 `verifin`) 是一个完全免费、数据自主、本地优先的 Flutter 记账应用。仓库规则详见 `AGENTS.md`（中文），本文件是其摘要加架构说明。**与用户沟通时使用中文。**

## 常用命令

```bash
flutter pub get                        # 安装依赖
flutter run -d <android-device-id> --flavor github   # 本地预览（定义了 flavor，必须带 --flavor）
flutter analyze                        # 静态检查（flutter_lints）
flutter test                           # 运行全部测试
flutter test test/entries_test.dart    # 运行单个测试文件
flutter test --plain-name "关键字"      # 按名称运行单个测试
dart format .                          # 提交前格式化
```

电脑端 UI 方案使用独立的 `tool/ui_lab` Flutter Web 工程预览：

```bash
cd tool/ui_lab
flutter pub get
flutter run -d chrome
```

该工程只用静态演示数据并复用纯主题/纯展示代码，不导入产品入口、Controller、数据层或平台桥；根应用仍无 `web/`，正式应用的本地预览与验收只能在 Android 模拟器或真机上进行。UI Lab 单独执行 `flutter analyze`、`flutter test` 与 `flutter build web`，正式验收仍使用 GitHub CI 构建的 release APK（下载到手机安装测试）。

## 发布与 CI

- **不要在本机构建 APK 作为交付物**。Android 安装包只由 GitHub Actions 生成。
- 质量 CI（`.github/workflows/ci.yml`）在 PR 与 push 到 `main` 时执行 format check → analyze → test。发布 CI（`.github/workflows/flutter.yml`）只在推送 `vX.Y.Z` 标签时触发：analyze → test → `build apk --release --target-platform android-arm64 --flavor github` + `build appbundle --release --flavor play --dart-define=SELF_UPDATE=false` → 发布 GitHub **预发布**（`prerelease`、不标记 Latest）。**真机验收通过后，在 GitHub 手动把该 release 改为正式版（取消预发布、设为 Latest）**——CI 默认只出预发布，不直接出正式版。
- **分发渠道 flavor（`github` / `play`）**：Play 政策更严，`android/app/src/play/AndroidManifest.xml` 用 `tools:node="remove"` 移除两个 Play 不允许的权限——① `REQUEST_INSTALL_PACKAGES`（应用内自更新「下载 GitHub Release APK 并安装」用，Play 禁止应用自更新）；② `USE_EXACT_ALARM`（Play 仅限「闹钟/日历」类应用，记账应用不符合，Play 版改用 `SCHEDULE_EXACT_ALARM` 这个可申请的精确闹钟、未授权自动回退 inexact）。自更新入口另由 CI 的 `--dart-define=SELF_UPDATE=false` 驱动 [lib/app/build_config.dart](lib/app/build_config.dart) 的 `kSelfUpdateEnabled` 在设置页隐藏。GitHub 自分发不受 Play 政策约束，两权限都保留（体验更好）。**定义 flavor 后所有本地 Android 构建/运行都必须带 `--flavor github`**（`flutter test`/`analyze` 不受影响）。GitHub 版 APK 走 github flavor、Play 版 AAB 走 play flavor；新增「仅某渠道」的权限/行为差异一律走 play flavor 清单 + `kSelfUpdateEnabled` 这类开关表达，勿在 main 清单里放 Play 禁用的权限。
- **只出 arm64-v8a 单架构包**（覆盖 2019 年后绝大多数机型、比 universal 约减半，实测 ~101MB→~55MB；极老 32 位设备装不了，已在 Release 说明注明并附「更新前先备份」）。release 开启 R8 `isMinifyEnabled`+`isShrinkResources`（`android/app/build.gradle.kts`），裁掉未用插件代码与未引用资源；**反射依赖点由 `android/app/proguard-rules.pro` 的 keep 规则保护**（ML Kit 识别器 + `flutter_local_notifications` 的 Gson 序列化 `com.dexterous.**`）——minify 开启后新增依赖若用反射，必须补 keep 规则，否则仅 release/CI 崩溃或功能静默失效。
- 发版用 `scripts/publish.sh patch`（也支持 `minor`、`major` 或显式版本号）；Windows 使用等价的 `./scripts/publish.ps1 patch`。脚本要求启动时工作树完全干净，会更新 `pubspec.yaml` 和 `lib/app/app_version.dart`、执行 format/pub get/analyze/test、创建 `chore: release vX.Y.Z` 提交、打标签并推送；改动发布流程时须同步维护两份脚本。
- **发版必须经用户明确同意**：只有用户明说「发布 patch/minor/major 版本」之类时才执行发版流程，绝不擅自发版；打标签会触发 CI 构建，不可逆。
- 发版前先把 `CHANGELOG.md` 顶部的 `## [Unreleased]` 段落改名为本次版本号并加日期（如 `## [1.8.2] - 2026-07-08`），再在其上新开一个空的 `## [Unreleased]`，**先单独提交 CHANGELOG 变更**；确认当前分支为 `main` 且工作树完全干净后，再运行发布脚本。
- Release APK 必须用项目内稳定 keystore（`android/app/verifin-release.jks`）签名。

## 提交规范

- 提交信息用 `type: summary` 格式（如 `feat: add login screen`、`fix: correct counter test`）。
- **提交中不要包含任何 AI 署名**（`Generated by ...`、`Co-authored-by: ...` 等）——这是仓库明确规则，覆盖默认行为。
- 每完成一个相对独立的模块就提交一次，不要把所有改动混到最后一次提交。

## 架构

### 状态管理：单一 Controller + InheritedNotifier

全部应用状态集中在 [lib/app/veri_fin_controller.dart](lib/app/veri_fin_controller.dart) 的 `VeriFinController`（`ChangeNotifier`）中：交易、账户、分组、账本、分类、预算、个人信息、主题和各种视图偏好。它通过 [lib/app/veri_fin_scope.dart](lib/app/veri_fin_scope.dart) 的 `VeriFinScope`（`InheritedNotifier`）注入组件树。没有 Provider/Riverpod/Bloc 等第三方状态库。

- 持久化：账目类核心数据（交易、账户、分组、账本、分类、标签、周期规则、汇率、月度/分类/按日预算）**只**存 SQLite，由 [lib/data/](lib/data/) 的 `AppDatabase`（建表 + 版本迁移）与 `LedgerRepository`（接口，`saveX` 语义为「落库后该表内容 == 传入列表」）承载，生产实现 `SqliteLedgerRepository`。**写入为行级差分**：交易/账本/账户/分组/分类/标签/周期规则/汇率各在内存保留「上次落库的行快照」，`saveX` 只写与快照不同的行（`_incrementalReplace`），避免单条改动就删全表再全量重插（`entries` 随交易量无限增长、是写放大主因）；附件表（含大 JPEG blob，缓存整行会成倍占内存）与预算表（极小）仍整表覆盖；交易详情的本体、关联退款和附件例外走 `saveEntryAggregate`，在一个 SQLite 事务内同时替换 `entries`/`attachments` 并重建交易快照，保证聚合编辑原子落库；导入/恢复/重置/删账本走 `replaceAllLedgerData` 多表原子整替（整替后重建各表快照基线）。controller 内存列表仍是读取源，写入经 `_persistX` 落库。`VeriFinController.create()`（唯一构造入口，异步）从库载入；全新数据库首启动播种默认账本/账户/分组/分类（标签默认为空）。交易与标签为多对多，交易表 `tag_ids` 列存 JSON 数组，不建关联表；标签全局共享不分账本。图片附件存独立 `attachments` 表（按 `entry_id` 关联，`data_url` 存压缩 JPEG），不放 entries 表以避免整表覆盖写入放大；删除交易/账户/账本时级联清理附件。没有 KV↔SQLite 迁移，也没有 KV 回退——账目类只认库。偏好类小数据（主题、触感、面板配置、资产排序、封面、活动账本、个人资料、金额小数位等）仍以 JSON 存入 `LocalKeyValueStore`，键形如 `verifin.theme.v1`；资产页普通浏览折叠只存在页面内，不再持久化。金额显示偏好复用 `verifin.amount_format.v1`（**进备份**），语义为 `CurrencyFractionStyle.standard/compact`；旧 `amountForceTwoDecimals` 只作为兼容入口。无 context 的金额格式化统一读取 [lib/app/amount_format.dart](lib/app/amount_format.dart) 顶层状态，并按 ISO 4217 minor unit 输出。修改 `lib/data/` 表结构须提升 `AppDatabase.schemaVersion`、同步 `_schemaCurrent` 并在 `_migrations` 注册表登记「升到 vN」的迁移函数（缺段升级即抛错；勿改历史段），同时更新 `test/migration_matrix_test.dart`（从每个历史版本升级并与全新库比对结构）与 `test/model_roundtrip_test.dart` 夹具（模型加字段时必改，锁定 toJson/fromJson/toRow/fromRow 四处映射不漂移）。测试注入内存实现 `InMemoryLedgerRepository`（见 `test/support/`；`test/repository_contract_test.dart` 对内存/SQLite 两实现共跑同一组契约断言，改仓储语义须让契约仍绿）。分类有参照完整性保护（防「幽灵同名分类」，详见 `docs/dev/tech-decisions.md` 的「分类参照完整性」）：`categories` 表有 `(label,type,IFNULL(parent_id,''))` 唯一索引；`VeriFinController.create()` 载入与 `importFromJson` 覆盖后都跑 `_healCategoryData()` 自愈（孤儿 parentId 重挂顶级、重复同名分类合并并迁移引用、悬空交易/规则引用与空分类的收/支交易归入「未分类」——固定 id `uncategorizedCategoryId`，与导入兜底同一约定，issue #16）；创建分类（`addCategory`、导入 `resolveCategory`）按 `normalizedCategoryLabel` 归一化查重复用——新增分类创建入口须走此查重、勿绕过唯一约束。
- 多币种：`LedgerBook.baseCurrencyCode` 是预算、统计、AI 汇总和总资产的本位币；`Account.currencyCode` 是余额/额度单位；`LedgerEntry.amount` 是原币金额，`accountAmount`/`toAccountAmount` 是两端真实金额，`baseAmount` 是保存时冻结的本位币金额。跨币转账 `baseAmount == 0` 且不计收支，退款缓存改为 `refundedBaseAmount`。本地 `ExchangeRate` 方向固定为 `1 外币 = X 本位币`，只查交易日当天或之前的最近记录；修改汇率不重写历史交易。金额换算/格式化只走 `currency_math.dart`，用户界面金额使用 `formatUserMoney` / `formatSignedUserMoney`，聚合卡片用 `MoneyUnitLabel` 标明上下文；汇率 UI 用 `formatRateValue`，CSV 精确往返用 `formatRateValueExact`。缺率必须返回类型化状态，禁止 1:1 回退或展示部分资产总额。完整不变量见 `docs/dev/multi-currency-design.md`。
- 货币显示偏好：`MoneyUnitStyle.symbol/code` 与 `hideUnitInSingleCurrency` 分别存 KV `verifin.money_unit_style.v1` / `verifin.hide_single_currency_unit.v1`，进备份、初始化保留。Controller 根据当前账本的账户、交易、周期规则和汇率同步 `amount_format.dart` 顶层状态；单币种默认隐藏单个金额的重复单位，多币种始终显示用户选择的符号或代码。
- 多账本隔离：交易、账户、分组都带 `bookId`，controller 的 getter 按 `_activeBookId` 过滤后返回不可变列表；月份/分类预算的键带 `bookId:` 前缀（旧数据加载时自动迁移到默认账本）；按日预算是账本级的每日花销上限，键就是 `bookId`（`daily_budgets` 表），当日已花按 `dayExpenseTotal` 聚合。**默认预算 + 单月覆盖**（issue #21）：月度总预算与分类预算都支持「默认值（每月自动沿用）」——存于同表的**哨兵键** `bookId:default`（月度）/ `bookId:default:catId`（分类），逐月键恒为 `yyyy-MM` 纯数字故与 `default` 天然不冲突，**零 schema 变更、零迁移**（键 helper `_defaultMonthlyBudgetKey`/`_defaultCategoryBudgetKey` 在 [lib/app/veri_fin_controller.dart](lib/app/veri_fin_controller.dart)，账本删除 `startsWith('bookId:')`、分类删除/合并 `endsWith(':catId')` 的清理对默认键同样生效）。取数优先级：`monthlyBudget(月)`/`categoryBudget(月,cat)` = 单月覆盖 ?? 默认 ?? **0**（不再有历史硬编码 800）；`monthlyBudgetIsOverride(月)` 区分「沿用默认」与「本月单独」，`setDefaultMonthlyBudget`/`setDefaultCategoryBudget` 设默认、`setMonthlyBudget`/`setCategoryBudget` 设单月覆盖、`clearMonthlyBudgetOverride` 清覆盖回沿用默认。默认预算随预算表持久化并**进 JSON 备份**、按账本隔离、初始化随预算清空。UI 拆分为**总览页** `BudgetOverviewPage`（只读，右上角齿轮进设置；月预算下状态 chip 点按进 `showMonthlyBudgetOverrideSheet` 调本月覆盖；分类树只读且**默认折叠**）与**设置页** `BudgetSettingsPage`（配默认月预算/按日上限/周期起始日 + 可编辑的分类默认预算树），历史页月份行点按也走覆盖弹窗——文件 [lib/pages/budget_pages.dart](lib/pages/budget_pages.dart)（含 part `budget_settings_page.dart`）。**预算周期起始日**（issue #19）：每账本一个（1–28，默认 1=自然月；KV `verifin.budget_cycle.v1`、**进 JSON 备份**、初始化清除、删账本清引用），纯函数在 [lib/app/budget_cycle.dart](lib/app/budget_cycle.dart)——预算仍按「键月」`yyyy-MM`（周期起始日所在月）存取、`budgetCycleOfKeyMonth` 的窗口决定该期聚合哪些交易，startDay=1 时窗口与键完全退化为自然月、历史预算零迁移；**只有预算体系**（预算页、首页/看板预算面板、预算小组件、小组件预览页）按周期取数（controller 的 `budgetWindow`/`budgetKeyMonthFor`/`budgetCycleIsCustom`），统计报表仍按自然月；自定义周期时文案切「本期/上期」、标签展示日期范围（`budgetCycleRange`）；桌面预算小组件的自愈锚点为周期截止日（推送参数 `budgetExpiry`、原生 `KEY_BUDGET_EXPIRY` 按 ISO 日期字典序比较，旧 `budgetMonth` 月锚点仅作「更新后未打开」的过渡兜底）。
- 账户类型（`AccountType`）六种，两项能力各由一个 getter 判定、**勿硬编码具体类型**：`supportsCardLast4`（完整卡号 `Account.cardNumber` + 后四位 `cardLast4`）仅信用卡/储蓄卡；`supportsCredit`（信用额度 `creditLimit`、账单日 `statementDay`、还款日 `dueDay`、还款）仅信用卡/信用账户（`creditAccount`，花呗/白条等无实体卡号的信用账户）。「后四位跟随完整卡号」开关是**持久化字段** `cardLast4Follows`（落库 + 进备份，忠实还原用户选择、不靠反推），由受控组件 `CardNumberFields`（`follows`/`onFollowsChanged`）驱动、打开时后四位自动取末四位（`cardLast4Of`）；列表只显示后四位、详情页展示完整卡号可复制。信用额度/本期账单纯函数在 [lib/app/credit_card.dart](lib/app/credit_card.dart)（`usedCredit`/`availableCredit`/`currentBillingCycle`/`billingCycleExpense`）；还款（`CreditRepaymentPage`）本质是「扣款账户→本账户」的转账（使用转账分类，扣款账户可为空表示代还）。切换类型丢失对应能力时清空相关字段。完整能力矩阵与取舍见 `docs/dev/tech-decisions.md`「账户类型能力矩阵」。
- 转账（`EntryType.transfer`）同时影响转出/转入账户余额，但不计入收支统计（聚合逻辑在 [lib/app/ledger_math.dart](lib/app/ledger_math.dart)）。转账可带手续费 `LedgerEntry.fee`，由转出账户承担：转出扣 `accountAmount + fee`，转入加 `toAccountAmount`，两端可为不同币种。支出可标记待报销（`reimbursable`）；**退款是挂在原支出上的独立条目**（`EntryType.refund` + `refundOf` 链接 + 可空 `settledAt` 到账日期，UI 在 `refund_editor.dart` 的 `RefundSection`/`showRefundSheet` 与 `pending_refunds_page.dart`），类比转账不计普通收入/支出，支持多笔 / 跨币账户到账 / 待到账·已到账两态（cash basis：待到账不进余额与统计）。交易详情把本体、退款和附件作为同一个草稿，最终通过 `saveEntryAggregateDraft` 提交；该 API 忽略 UI 传入的旧退款缓存，仅按已到账退款的 `baseAmount` 重新派生 `refundedBaseAmount`。统计净额按 `LedgerEntry.netBaseAmount`（`baseAmount - refundedBaseAmount`，floor 为 0）；账户余额按账户实际金额计算，即「支出扣 `accountAmount` + 退款条目入 `accountAmount`」。退款只能从「原支出→添加退款」创建（`EntryType.userSelectable` 排除 refund）、且不进通用交易列表（净额已在支出行体现、带「已退」标记）。完整设计与决策见 `docs/dev/refund-design.md` 与 `multi-currency-design.md`。交易的 `accountId` 可为空串表示「无账户」（只记金额、不计入任何账户余额但仍按 `baseAmount` 计入收支；转账 `toAccountId` 也可空，如单边转账）——展示层须用 `accountDisplayName`（`model_lookup.dart`）而非 `accountById`，避免空 id 回退首个账户误显示。

### 平台适配：条件导入两件套

Android/测试差异统一用条件导出模式（`stub` + `if (dart.library.io)`）：

- [lib/local_storage/](lib/local_storage/)：`LocalKeyValueStore`（偏好类小数据）— io 用 `SharedPreferences`，测试用内存 stub。真实平台启动必须 `LocalKeyValueStore.create()`，**真实平台绝不能落到内存实现**。
- [lib/data/database_factory.dart](lib/data/database_factory.dart)：SQLite `DatabaseFactory` 与路径解析 — io 用 `sqflite` 原生、stub 抛错（测试注入 `databaseFactoryFfi`）。
- `lib/app/avatar_picker_*.dart`：本地图片选择。
- `lib/app/data_file_port_*.dart`：备份导出/导入的文件读写（Android 默认写系统 Downloads）— io 支持文本写入（`downloadTextFile`，CSV 模板等）与字节写入（`downloadBytesFile`，zip 备份），导入用 `pickBackupBytes` 按字节读入（`.json`/`.zip` 均可）。
- `lib/app/attachment_picker_*.dart`：交易图片附件的拍照/相册选择 — io 用 `image_picker` 拍照或选图并压缩为 JPEG data URL（`image` 包在后台 isolate 缩放），stub（测试宿主）不支持（`attachmentPickingSupported=false`）；UI 组件 `lib/pages/attachments_editor.dart`。
- `lib/app/biometric_auth_*.dart`：应用锁的生物解锁 — io 用 `local_auth`（仅 Android/iOS 生效，不保存生物特征数据；`local_auth` 无法严格排除人脸，故用户文案统一为「生物解锁」），stub（测试宿主）一律不可用。Android 端依赖 `FlutterFragmentActivity`、`USE_BIOMETRIC` 权限与 `minSdk≥23`。
- `lib/app/backup/backup_storage_*.dart`：备份目录选择与文件读写 — io 上 Android 走 SAF（经 `platform_bridge`）、桌面走 `file_selector`+`dart:io`，stub 不支持持久目录。
- `lib/app/backup/webdav_client_*.dart`：WebDAV 备份 — io 用 `dart:io HttpClient` 手写 PUT/GET/PROPFIND/MKCOL，stub 不支持。
- `lib/app/reminder/notification_scheduler_*.dart`：记账提醒本地通知 — io 用 `flutter_local_notifications`+`timezone`+`flutter_timezone`，每日在设定时刻发一条提醒（`matchDateTimeComponents: time` 每日重复），stub（测试宿主）不支持。**用精确闹钟 `exactAllowWhileIdle`**：inexact 调度在 Doze / 国产 ROM 后台限制下常被系统无限推迟、根本不显示（历史「设了提醒却收不到」的根因），精确闹钟休眠下也能准时触发；`requestPermission` 一并申请精确闹钟权限，精确权限缺失时 `apply` 自动回退 inexact。设置页有「发送测试通知」按钮（`showTest` 立即 `show`）供当场验证通知能否显示（区分权限/渠道问题与定时不触发问题）。配置 `ReminderSettings`（`reminder/reminder_settings.dart`）存 KV（`verifin.reminder.v1`），是设备本地偏好、不进 JSON 备份；`main.dart` **开屏、每次回前台（`resumed`）与配置变化时都 `apply` 重排通知**——每日提醒底层是「触发时自排下一天」的精确闹钟链，被 Doze / force-stop / 重启 / 时区变化打断后不会自愈，只在冷启动重排会漏掉「常驻后台、只热恢复」的用户，故每次回前台幂等重排（`apply` 先 `cancel` 再排）对齐一次。`_ensureTimezone` 取本地时区失败时**不**置 `_timezoneReady`（下次 apply 会重试），避免早期通道未就绪等瞬时失败让 `tz.local` 永久停在 UTC、提醒按 UTC 时刻错点触发。Android 端需 `POST_NOTIFICATIONS`/`RECEIVE_BOOT_COMPLETED`/`USE_EXACT_ALARM`+`SCHEDULE_EXACT_ALARM` 权限（`USE_EXACT_ALARM` 在 13+ 自动授予、GitHub 自建分发不受 Play 政策限制；**Play 版 play flavor 移除 `USE_EXACT_ALARM`**，Play 仅限闹钟/日历类应用用它，记账应用改靠用户可授权的 `SCHEDULE_EXACT_ALARM`、未授权回退 inexact）、插件接收器与 core library desugaring。国产 ROM force-stop / 后台限制仍可能拦截，需用户加省电 / 自启动白名单，代码内无法根治。
- `lib/app/ai/ai_client_*.dart`：AI 对话记账的聊天补全请求 — io 用 `dart:io HttpClient` POST `{baseUrl}/chat/completions`（OpenAI 兼容、`Authorization: Bearer`、带超时），stub（测试宿主）抛错。配套 `ai_settings.dart`（`AiSettings` 值类：baseUrl/apiKey/model，`chatCompletionsUrl` 拼接容错）与纯函数 `ai_entry_parser.dart`（`buildAiEntryPrompt` 组提示词，`parseAiEntryDraft` 从模型文本提取 JSON 并把分类/账户 id 校验到当前账本清单，未命中降级并回 `AiDraftWarning`，产出 `AiEntryDraft`；提示词带当前日期与时间，解析 `date`(YYYY-MM-DD)+可选 `time`(HH:MM) 合成 `occurredAt`，用户没提时间就沿用此刻的时分）。首页「记一笔」按钮行为由 `FabActionMode`（`verifin.fab_action.v1`，默认 manual）决定：manual 走数字键盘 + `EntryDetailPage`，ai 走 `ai_entry_sheet.dart` 的自然语言输入 → 解析草稿 → `EntryDetailPage(initialDraft:)` 预填确认（AI 不直接落账），manualTapAiLongPress 则点击手动、长按 AI（首页 FAB 用 `Material`+`InkWell` 自绘、单个 InkWell 同持 `onTap`/`onLongPress`——`FloatingActionButton` 内部 InkWell 会吞掉外层 `GestureDetector` 的长按，故不用它；`shell.dart` 的 `_startQuickEntry(longPress:)` 按手势解析动作）。记账默认付款账户由 `verifin.default_account.v1`（KV，`Map<bookId,accountId>`，每账本各一个、**进 JSON 备份**、初始化随账户清空、删账户时清引用）承载，`controller.defaultAccountId` 校验账户存在且未隐藏后返回：手动记账把它作为 `EntryDetailPage(initialAccountId:)` 默认账户；AI 未识别到账户（`accountId` 空）时也回落到它，未设则记为「无账户」。设置入口两处：「我的 → 设置 → 默认账户」（账户选择弹窗，可选「无默认」）与账户详情页 `CompactSwitchRow`「设为默认账户」。数字键盘 `NumberPadSheet` 支持四则运算算式（如 `500+800`）：纯函数 `lib/app/calc_expression.dart` 的 `evaluateAmountExpression` 按标准优先级求值（含算式返回 null=不完整），面板右下角浅色展示结果预览，不完整时提示「算式不完整」并禁用确认。`AiSettings` 存 KV（`verifin.ai.v1`，明文本机、不进 JSON 备份、初始化保留），设置页在「我的 → 设置」，子页 `AiSettingsPage`（`ai_settings_page.dart`）配置并测连通性。Android 已有 `INTERNET` 权限，并开启 `usesCleartextTraffic="true"` 以支持本地 / 自建 `http://` 端点（如局域网 Ollama / LM Studio），云端 https 端点同样可用。
- **AI 财务 Agent**（区别于上面的「AI 记账」）：让用户自然语言问账目，Agent 自主调用只读工具查询当前活动账本，并以图表 / 列表 / Markdown 作答。`ai_agent_engine.dart` 是最多 5 轮 / 10 次工具调用的结构化状态机；`ai_native_tool_protocol.dart` 与 `ai_prompt_tool_protocol.dart` 分别实现原生 Tool Calls 和带边界标记的兼容协议，自动模式优先原生并在明确不支持时安全降级。`ai_tool_schema.dart` 是两种协议共用的强类型参数单一事实来源；`ai_query_tool.dart` 提供 `AiQueryTool`、`AiToolContext`、`AiToolResult` 与 `buildAiQueryTools()`，**工具全部是只读纯函数**；`ledger_query.dart` 提供通用交易筛选。`ai_client_*.dart` 通过 `aiAgentStream` / `aiAgentComplete` 产出正文、推理、工具调用和结束状态等结构化事件，校验 SSE 完整性、处理空闲超时并限制重试。UI 由 `ai_chat_page.dart`、`ai_agent_step_view.dart` 和 `ai_result_view.dart` 组成，只展示最终正文、可展开的工具步骤与结果卡，不展示推理、原始工具 JSON 或底层异常。聊天历史 KV `verifin.ai_chat.v1` 保存文本、已完成步骤和序列化结果卡片；能力缓存 KV `verifin.ai_capabilities.v1` 按 endpoint + model 保存且不含 API Key，两者均仅存设备、不进备份。**工具登记与维护约定见 `docs/dev/ai-tools.md`。**工具产出的卡片标题为纯函数暂留中文（其余 UI 已 i18n）。
- `lib/app/screenshot_recognizer_*.dart`：截图识账的端上 OCR — io 用 `google_mlkit_text_recognition`（中文模型、离线，图片不出设备；按字节识别时落临时文件走 `InputImage.fromFilePath`，识别完即删），stub（测试宿主）不支持。**坑**：该插件对各脚本识别库只 `compileOnly`，App 必须在 `android/app/build.gradle.kts` 显式 `implementation("com.google.mlkit:text-recognition-chinese:...")`，并在 `proguard-rules.pro` 给未引入的梵/日/韩脚本加 `-dontwarn`——否则 debug 正常、**release 的 R8 报缺类构建失败**（CI 才暴露）。**定案：应用永久不做监听类自动记账**（不注册 NLS/无障碍/短信，见 `docs/dev/auto-capture-plan.md`），采集一律由用户主动触发：① 其他 App 分享图片/文本给 Veri Fin；② 自动化工具（Tasker 等）显式发 `top.talyra42.verifin.action.CAPTURE_TEXT`（extra `text`，用户文档 `docs/automation.md`）。两者都进 `ShareReceiverActivity`（无界面跳板：`ACTION_SEND` 会把目标起在分享方任务栈，直指 MainActivity 会双 Flutter 引擎，故转发回自家任务栈后立即 finish），MainActivity 暂存后经 `verifin/app` 通道 `consumeCaptureImage`/`consumeCaptureText` 被拉取（运行中由 `openSharedCapture` 通知，冷启动由 shell 开屏主动 consume，仿快速记账 intent 模式；原生侧文本限 8000 字、图片限 25MB）。Flutter 流程在 `lib/pages/capture_entry.dart`：AI 硬门槛（未配置先引导）→ 模态加载态 → OCR → `ai_entry_parser.dart` 的 `requestCapturedEntryDraft`（`buildCapturedEntryPrompt` 在基础提示词上追加 OCR 噪音/忽略余额/交易时间优先/非交易置 0 规则；`capturedTextLikelyTransaction` 无数字短路不调 AI、超 4000 字截断）→ `EntryDetailPage(initialDraft:)` 确认，**绝不静默落账**；分享图不落库不留存。App 内入口：AI 记账弹层的「截图识账」按钮（相册选图，`pickScreenshotPath`）。

[lib/app/platform_bridge.dart](lib/app/platform_bridge.dart) 是 Android MethodChannel 桥，出站能力按域拆为五个 Bridge 类（part 文件，import 入口不变；新增原生能力放对应域勿再堆一类）：`AppCaptureBridge`（磁贴+分享采集）、`AppUpdateBridge`（更新检查下载）、`AppWidgetBridge`（小组件推送/固定）、`AppStorageBridge`（下载目录+SAF）、`AppSecurityBridge`（FLAG_SECURE）；入站调用受「一个 channel 只能挂一个处理器」约束、统一在库头分发（快速记账磁贴入口、分享/外部采集内容消费（`consumeCaptureImage`/`consumeCaptureText`）、GitHub Release 更新检查与下载（`checkLatestRelease`/`downloadLatestUpdate` 带 `includePrerelease` 参数：稳定通道用 `/releases/latest`，预发布通道拉 `/releases` 列表剔草稿后取版本号最高的一个；结果回传 `isPrerelease` 供下载前风险提示，更新弹窗有「包含预发布版本」开关）、备份目录 SAF 读写、桌面小组件数据推送 `updateWidgetData`、一键固定小组件 `pinWidget`）。桌面小组件有三个：今日支出+记一笔（`QuickEntryWidgetProvider`）、本月可用预算（`BudgetWidgetProvider`）、资产总额（`NetWorthWidgetProvider`，后两者继承 `StatWidgetProvider` 共用 `stat_widget` 布局）；数据经 `WidgetData`（原生 SharedPreferences 读写 + 广播刷新）承载，Flutter 侧 `home_widget_service.dart` 的 `pushWidgetData` 在开屏/回前台/记账后一次推送三者的值（均按用户偏好格式化）。**跨天/跨月自愈**：推送时额外带上「今日支出」对应日期（`todayDate`）+归零值、「本月可用」对应月份（`budgetMonth`）+整月预算值；原生渲染时若锚点日期/月份已过期就自动展示归零值/整月预算（`WidgetData.todayAmountForToday`/`budgetForMonth`），不必等应用打开重新推送。`WidgetRefreshReceiver`+`WidgetRefreshScheduler` 用 `AlarmManager.setAndAllowWhileIdle`+`RTC_WAKEUP`（无需精确闹钟权限）在每晚午夜刷新一次全部小组件触发自愈，并在推送/重绘/开机（`BOOT_COMPLETED`/`MY_PACKAGE_REPLACED`）时重排下一晚闹钟；**必须用 allow-while-idle+wakeup**——普通 `set(RTC,…)` 在 Doze 下整夜被压制、不唤醒设备，会导致午夜闹钟不触发、次日打开手机仍是旧数据（历史 bug）。两个时间敏感小组件（今日支出/本月预算）的 `updatePeriodMillis` 设为 30 分钟作为「设备活跃时」的第二条兜底触发（不唤醒设备、搭已有唤醒周期的车），资产总额不随时间过期故不需锚点也不设周期。国产 ROM force-stop 会取消闹钟且收不到广播，此场景无法在代码内根治，靠用户加自启动白名单或次日开 App 恢复。应用内小组件展示页 `lib/pages/widget_gallery_page.dart`（我的 → 数据与工具 → 桌面小组件）预览各小组件样式、经 `pinWidget` 请求系统「添加到桌面」（启动器不支持时回落手动添加引导）。小组件外观为随系统深浅色切换的白/黑卡片（`res/values(-night)/colors.xml` 的 `widget_*` 颜色 + `widget_background` 描边卡片，不用大面积渐变），强调色仅用于「记一笔」按钮。点「记一笔」复用快速记账 `ACTION_QUICK_ENTRY`。新增小组件类型必须在编译期声明 `AppWidgetProviderInfo` XML + Manifest receiver（运行时无法凭空新建）。

### 数据管理与备份

数据管理页（`DataManagementPage`，我的页进入）集中导出/导入/初始化，以及 `lib/app/backup/` 备份子系统：`backup_settings.dart`（目录/频率/保留/上次时间 + 纯保留清理逻辑）、`backup_archive.dart`（zip 打包/解包纯函数：把图片附件字节从 JSON 剥离、单独存 `attachments/<id>`，与 `backup.json` 一起打进 zip，解包拼回 `dataUrl`；`looksLikeZipBytes` 按魔数识别）、`backup_crypto.dart`（AES-GCM+PBKDF2 加密，`cryptography` 包）、`backup_service.dart`（编解码唯一收口：写出侧 `prepare` 决定格式——未加密→zip 字节 `.zip`、加密→文本信封 `.json`；导入侧 `decodeBackupBytes` 是**唯一字节解码入口**（zip 解包/加密信封待解密/明文直通，`decryptEnvelope` 解密），controller 只认明文 JSON（`exportDataJson`/`importDataJson`）、不碰字节格式；另含写文件+回读校验+保留清理）、`backup_coordinator.dart`（打开/回前台/记账后触发本地目录与 WebDAV 自动备份，`main.dart` 注入 `onEntryAdded` 与生命周期）、**导入子系统实现在 `lib/app/backup/import/`**（各来源解耦、分文件）：每个来源一个 parser 文件（`alipay`/`wechat`/`mint`/`yimu`/`qianji`/`tally`/`csv_template.dart`），签名 `Uint8List → ParsedImport`（强类型 `RawImportRecord` + 逐行错误 + 可选账户元数据），**各认自己的格式、互不共用表头识别**；`raw_import.dart`（记录类型 + 字符串→强类型解析原语 `parseImport*`/`buildRecordFromStrings`）、`text_format.dart`（`parseCsv`/`parseTsv`/字节解码/`excelSerialToDateTime` 等格式级工具）、`xlsx_reader.dart`（手写 xlsx 正则解析）；`plan_builder.dart` 的 `buildImportPlanFromRecords` 是**唯一共享落库计划生成器**（按名建账户/分类层级/标签、构造 `LedgerEntry`、退款钳制、Tally 账户余额回推、**缺失分类兜底到固定 id 的「未分类」**（与 `_healCategoryData` 自愈同一约定、不再产出空 `categoryId`，预览页有该候选时分类映射区默认展开，issue #16）——通用领域逻辑只此一份、绝不按平台复制，否则必漂移；账户匹配按**去首尾空格的名称**、仅恰好一个现有账户同名时才复用，多个同名不猜归属——账户名不唯一、id 才是身份——转为待新建候选交预览页「导入账户」映射区由用户显式决定；创建/编辑账户 `addAccount`/`updateAccount` 与导入落库同样对名称 trim）。两个历史 **facade** 供外部 import（外部只认这俩、内部实现拆在 `import/`）：`transaction_import.dart`（re-export + `buildImportPlan(rows)` 兼容入口；CSV 模板 `csvTemplateColumns` 为模板列单一真源，`validateCsvTemplateHeader` 用**白名单**校验表头只允许模板列、混入第三方原生表头即报错，不再靠通用表头猜测识别钱迹/随手记）、`payment_import.dart`（`ImportPlatform` 枚举 + `parsePlatformBytes` **注册表** + `buildPlatformImportPlan`；**新增导入平台 = `ImportPlatform` 枚举值 + `parsePlatformBytes` 注册 case + parser 文件，三处同步**）。各来源解析细节：支付宝 GBK CSV（「交易分类」列→分类）、微信 xlsx（日期 Excel 序列号经 `excelSerialToDateTime` 直接转 DateTime；「交易类型」列（商户消费/微信红包…）→分类候选，交预览映射区归到用户体系，issue #16）、薄荷 UTF-16 制表符 CSV、**钱迹 UTF-8（含 BOM）CSV**（18 列明细：退款按「关联账单」折叠进原支出退款额、报销/报销记录记为支出+收入、0 元心愿单静默跳过；**债务/借贷类（借出/收款/借入/还款/利息）一律跳过**——Veri Fin 无债务功能、无法忠实表达）、一木记账 .xls 各自适配，多编码解码用 `charset`，微信 xlsx 用 `archive` 解压 + 正则解析 XML、日期转 Excel 序列号，一木 .xls 用自写 `xls_reader.dart` 读 OLE2+BIFF8（一木「账单导出(收支)」与「转账还款导出」是两个不同表头的文件、且还有其他导出，故拆成 `yimuBill`/`yimuTransfer` 两个入口由用户显式选，各只认自己的表头、选错即报错不猜测；账单文件的支出可带「退款」列（部分/全额退回）→映射到 `LedgerEntry.refundedAmount`（净额=金额−退款、退款回原账户，钳制在 `[0,金额]`）——规范列 `_canonicalHeader` 末尾追加了「退款」列、`_headerAliases` 注册 `refunded` 别名；账单还把一级「类别」+二级「二级分类」还原成父子层级（`buildImportPlan.resolveCategoryHierarchy`，子分类去重带 `parentId`、与唯一索引对齐）、逗号（半/全角）分隔的「标签」列拆成多标签（`resolveTags` 归一化去重复用/新建 `Tag`）、备注原样导入（issue #11；`_canonicalHeader` 末尾另追加「子分类」「标签」两列、`_headerAliases` 注册 `subcategory`/`tags` 别名，其他平台留空按名解析故向后兼容）；转账文件带转出/转入账户与手续费→映射到转账 `fee`；`tally` Tally 记账「备份」zip 用 `archive` 解压取 `backup_data.json`、`jsonDecode` 后逐条归一，交易时间为 epoch 毫秒故无损精确到秒，`assetId` 经 assets 数组映射为账户名，分类取二级分类叶子回退一级，转账 type=2/分类「资产互转」的转出/转入账户编码在 `note`「转出 -> 转入」里、按此拆解，选 zip 而非其 CSV「账单」导出是因后者只有日期无时分；**Tally 还导入 assets 数组里的账户余额与类型**——普通来源只由交易派生账户、余额从流水算出，Tally 走 `buildPlatformImportPlan` 里的专用分支 `_applyTallyAssets`：对本次新建的账户回推 `initialBalance = 目标余额 − Σ交易增量` 使导入后显示余额对齐 Tally 当前余额，没有流水的资产（零余额钱包、借出/负债对象）也直接以当前余额补建；Tally `amount` 恒为正、符号由 type 决定（负债 1/分期 4 记负，资产 0/借出 2/理财 3 记正，与 Veri Fin「余额正负=资产/负债」一致），`isIncludedInTotal`→`includeInAssets`；补建的账户经 `ImportPlan.standaloneAccountIds`→`ImportPreviewResult.alwaysCreateAccountIds` 传给 `applyImportEntries`，使其即便无交易引用也会落库、不被「只建被引用账户」的过滤丢掉），各来源 parser 产出强类型 `RawImportRecord`（二进制来源如微信序列号、Tally epoch 毫秒直接构造 DateTime、金额本就是 double，**不经字符串往返**——那正是历史 bug 温床，如 issue #10 一木退款被二次相减）后交 `plan_builder`；解析基于用户真实样例，禁止编造格式）、`xls_reader.dart`（最小 OLE2 复合文档 + BIFF8 读取器，纯 `dart:typed_data`、无库依赖：取 `Workbook` 流、SST 含 CONTINUE 跨记录拆分、LABELSST/NUMBER/RK/MULRK 单元格；pub.dev 无 BIFF8 库故自写）、`webdav_config.dart`/`webdav_client_*.dart`（WebDAV）。导入入口为数据管理页「导入账单文件」：**先选平台（支付宝/微信/薄荷/一木·账单/一木·转账还款/钱迹/Tally 等第三方软件，或本应用「CSV 模板」；选择器按「记账软件/支付平台」与「CSV 文件」两组分开展示、第三方软件在上模板在下）再选文件**，并附各平台「如何导出账单」引导；平台选择器用真实软件品牌图标（`assets/import_icons/`，与账户图标 `assets/account_icons/` 不同）。**选文件后不直接落库，先进导入预览页**（`lib/pages/import_preview_page.dart`）：`controller.parsePlatformImport` 只解析出 `ImportPlan`，预览页把交易**按日期分组**（复用 `common_widgets` 的 `groupEntriesByDate`/`DateGroupHeader`/`TransactionListCard`）列出，可逐条排除或点进 `EntryDetailPage.draft`（草稿模式，返回改后条目而不落库）完整编辑；顶部可折叠的「导入账户 / 导入分类 / 导入标签」映射区可给每个待新建账户/分类/标签改名或映射到现有条目（按待新建 id 整体替换，对所有引用它的交易一次生效；标签映射会改写交易 `tagIds` 里的临时 id、把子分类的 `parentId` 也随父分类映射一并改写）。确认返回 `ImportPreviewResult`（含 `candidateTags`），`controller.applyImportEntries` 只创建被保留交易实际引用到的新账户/分类/标签（分类还会补建被引用子分类未存在的祖先候选，避免父分类不被交易直接引用而被过滤掉、致子分类悬空；外加 `alwaysCreateAccountIds` 里携带余额的独立账户，即便无交易也建）。携带余额的来源（Tally）在预览的「导入账户」区默认展开、每行右侧显示导入后余额；**只有账户、没有交易的备份也能导入**（预览页确认按钮显示「确认导入（N 个账户）」，`buildPlatformImportPlan` 不因 `records` 缺省而报错）。预览/编辑传「现有＋待新建」的合并账户/分类列表，全程零落库副作用。**以后新增导入平台一律走此预览流程。**备份目录/加密口令/WebDAV 配置存 KV，均不随初始化数据清除，也不进 JSON 导出（设备本地）。

多币种导入的当前增量口径：`RawImportRecord` 可携带原币、账户/转入账户币种、两端实际金额、本位币金额和文件汇率；`plan_builder` 会先用文件汇率、再用目标日当天或之前的本地汇率补齐三层金额，仍缺率的行形成 conversion issue 而不是按 1:1 猜测。账户匹配必须同时满足归一化名称和币种；退款列在 plan 阶段生成关联退款条目并派生 `refundedBaseAmount`，不再直接写旧缓存。本应用 CSV 支持上述列的导出/重导入；钱迹只读取真实 fixture 已证实的「币种」列，薄荷/一木未证实的字段不得臆造。预览确认前不落库，候选汇率默认不保存，只有用户开启后才随最终保留的交易写入。

应用锁（PIN/图案/生物解锁）：加盐哈希（`lib/app/app_lock.dart`）存 KV，UI 与门卫在 `lib/pages/app_lock_page.dart` 与 `app_lock_gate.dart`（门卫放在 `MaterialApp.builder` 覆盖根 Navigator，冷启动与回前台锁定）。

### 页面层

编辑型全屏页面统一采用“初始快照 + 页面草稿 + 明确保存”：Header 右侧使用 `SaveHeaderAction`（软碟图标），`UnsavedChangesGuard` 统一拦截 Header 返回、Android 系统返回和预测性返回；dirty 时提供“保存 / 不保存 / 取消”，保存失败留页。页面不得在开关、选择或拖动回调中直接写 Controller；通过领域窄接口（`save*Draft`）等待 SQLite/KV 成功后退出。分类、标签、账户分组排序使用 `SortModeHeaderActions` 的独立排序草稿。资产页普通展开/折叠是临时浏览状态；视图模式、背景、分区和账户顺序集中在 `AssetDisplaySettingsPage` 一次保存。删除、导入、立即备份、还款、余额调整、待退款核销和应用锁等明确业务命令仍在自身确认后立即执行。完整边界与页面审计见 [docs/dev/save-interaction-consistency-design.md](docs/dev/save-interaction-consistency-design.md)。

[lib/main.dart](lib/main.dart) 保留应用入口、根组件与跨域生命周期编排（初始化、开屏/回前台补记与重排、备份/提醒/小组件/应用锁挂钩）；领域状态和业务逻辑仍下沉到 Controller 与各领域模块。页面按功能拆分在 `lib/pages/`：`shell.dart` 是底部四 Tab 导航壳（首页/资产/看板/我的），`home_page.dart`、`budget_pages.dart`、`transactions_pages.dart`、`assets_pages.dart`、`reports_page.dart`、`profile_pages.dart`、`entry_detail_page.dart` 各自承载一个功能域，跨页面共享的底部弹窗/对话框在 `sheets.dart`。模型（含 JSON 序列化）按域拆在 `lib/app/models/`（交易/账户/账本/分类/资料/偏好六个文件），[lib/app/models.dart](lib/app/models.dart) 为 barrel、外部导入路径不变；通用组件在 `lib/app/common_widgets.dart`（按域拆为交易/展示/骨架/菜单/面板/表单六个 part，入口不变），菜单 part 提供通用 `VeriAnchoredMenuAnchor` / `VeriAnchoredMenuButton`，以及 2–8 项静态受控单选的轻量适配层 `VeriAnchoredChoice<T>`；动态/分区长列表仍使用 `showOptionSheet` 或领域 Picker。底部弹窗在 `lib/app/entry_sheets.dart`（组件体勿直接实例化，走 `pages/sheets.dart` 的 `show*Sheet` 包装），自绘图表及其交互组件（`InteractiveTrendChart`/`InteractiveBarChart`、数据气泡、命中测试）在 `lib/app/chart_painters.dart`，图表序列/坐标轴纯函数在 `lib/app/series_math.dart`，多级分类的树形查询/前序展开纯函数（邻接表 `Category.parentId`，带环检测）在 `lib/app/category_tree.dart`，周期记账的到期日推进纯函数在 `lib/app/recurring.dart`（`main.dart` 在开屏与回前台调用 `controller.applyDueRecurring(now)` 补记到期交易），首页概览卡（`HomeTrendPanel`）可自定义展示数据：指标计算纯函数在 `lib/app/home_metrics.dart`（`computeHomeMetric`/`HomeMetricContext`，21 个指标；`HomeTrendConfig` 存 KV `verifin.home_metrics.v1`，**进备份**、初始化保留），自定义页 `lib/pages/home_metrics_settings_page.dart`（收支统计页右上角「编辑」进入，点槽位弹窗选指标、可复位），走势窗口为累积展开（`cumulativeWeekWindowFor`，详情页 `IncomeExpenseStatsPage` 支持周/月/季/年视图切换：周·月·季按天窗口 `weekWindowFor`/`monthWindowFor`/`quarterWindowFor`，年按 12 个月聚合 `monthlyNetValuesForType`，年视图下方按月列出、点某月下钻回该月），记账自动识别的推断纯函数（`suggestEntry`：从用户自己的历史里按「金额接近度+备注相似度+时段」找最像的几笔，加权推断当前这笔的**类型/分类/标签/备注**——不只是分类；单笔支撑须是强匹配（精确金额或备注重合）才敢翻转类型，避免松散金额误判；**自行推断出的类型只能是支出/收入**——白名单 `_kAutoInferableTypes` 把退款与转账的历史整条排除在扫描外，既不参与类型投票、其备注/标签也不会被当样本带出：退款不是可选类型且无对应分类（推断出它会让记账页拿不到任何分类而崩溃白屏，issue #26），转账不计入收支统计、仅凭金额接近就翻成转账错得隐蔽。用户**手动**选定类型后（`forcedType`，必属 `EntryType.userSelectable`）不受白名单限制，仍在该类型内识别分类/标签/备注）在 `lib/app/category_suggest.dart`。`entry_detail_page.dart` 在开屏（金额已定）、改金额、改类型、输备注时调用，逐字段填充「用户尚未手动改过」的项（`_typeTouched/_categoryTouched/_tagsTouched/_noteTouched`），静默无提示；其 `build` 开头把 `_type` 归一化到 `EntryType.userSelectable`（本页无法表达退款）、取默认分类处一律空安全兜底而非 `.first`——**新增「按类型取列表首项」的代码必须同样兜底空列表**，否则该类型无分类时整页 build 抛异常直接白屏（issue #26）；AI 草稿模式与导入草稿模式整体关闭自动识别。自动识别有**总开关** `autoSuggestEnabled`（KV `verifin.auto_suggest.v1`，**默认开**、全局不分账本、**进备份**、初始化保留，设置页「记账」分组的开关行）——关掉后 `entry_detail_page` 的 `_autoSuggestEnabled` 恒假、不再预填任何字段，纯函数 `suggestEntry` 本身不受影响。所有图表都必须支持点击展示数据，规范见 `docs/ui-guidelines.md` 的“图表交互”一节。只有出现明确复用或复杂度上升时再抽取组件。

分类图标：内置图标 code→`IconData` 由 `icon_catalog.dart` 的 `iconForCode` 映射（`categoryIconCodes` 为可选清单；`demo_data.dart` 现在只放种子数据，按 id 查询/展示回退在 `model_lookup.dart`）；**另支持 emoji 自定义图标**，以 `emoji:` 前缀存进 `Category.iconCode`（如 `emoji:🍜`，助手 `isEmojiIconCode`/`emojiOfIconCode`/`emojiIconCode`）。**分类图标一律用 `common_widgets.dart` 的 `CategoryIconBox`（带色块背景）或 `CategoryGlyph`（裸字形，Chip/内联用）渲染**，二者自动区分内置图标与 emoji——新增分类图标渲染点不要直接 `iconForCode(category.iconCode)`，否则 emoji 分类会回退成钱包图标。选择器 `showCategoryIconPickerSheet`（`sheets.dart`）为图标网格 + 常用 emoji 快选 + 自由输入。emoji 走 `iconCode` 存储，无 schema 变更、导出/导入天然覆盖。

统计分析页（`report_analysis_page.dart`）的排行支持三种分组维度（`_ReportGrouping`）：顶级分类（可点行下钻看子分类拆分，弹层子分类行与底部「查看交易」都跳到按该分类预筛的 `TransactionsPage(initialCategoryId:)`，与分类管理「查看交易」同一路径）、子分类（按记账所选分类，`reportCategoryStatsByOwn`；点行直接跳转同上）、标签（`reportTagStats`，一笔计入其每个标签、占比可重叠）；纯函数都在 `report_analysis.dart`。

### 国际化

使用 Flutter 内置 gen-l10n：ARB 文件在 `lib/l10n/`（`app_zh.arb` 为模板 + `app_en.arb`），`flutter pub get` 时自动生成 `AppLocalizations`（生成文件同目录，已提交）。**新增用户可见文案必须写入 ARB（zh + en 同步）并通过 `AppLocalizations.of(context)` 引用，绝不硬编码中文**。存量文案已全部迁移完成（2026-07）：应用支持中英双语，设置页「语言」可选跟随系统/简体中文/English（`LocalePreference` 存 KV `verifin.locale.v1`，设备本地、不进备份、初始化保留），`main.dart` 经 `localePreferenceListenable` 驱动 `MaterialApp.locale`。枚举显示名统一为 `label(AppLocalizations)` 方法；无 BuildContext 场景（桌面小组件、本地通知、生物识别系统弹窗）经 `lib/app/l10n_outside_context.dart` 的 `l10nForPreference` 按偏好解析。种子数据（默认账本/分类/简介）按首启动语言播种（`VeriFinController.create` 的 `systemIsEnglish`），播种后属用户数据不再切换。有意保留中文：法律文档正文、银行/品牌图标名、CSV 模板表头与逐行导入错误、无 context 的网络/桥接错误消息（有意暂缓，未英文化）。widget 测试脚手架预置中文（`makeController` 写 `verifin.locale.v1=zh`），直接 pump 单页的测试用 `zhMaterialApp`。真机验证清单见 `docs/dev/i18n-verification.md`。

### 测试

测试按领域拆分在 `test/`（`navigation_settings`/`accounts_assets`/`entries`/`budget`/`panels`/`backup`/`controller_unit`/`pure` 等 `*_test.dart`），共享脚手架在 [test/support/](test/support/)：`test_harness.dart` 提供 `makeController`/`pumpApp` 等助手，`in_memory_ledger_repository.dart` 是注入控制器/widget 测试的内存仓储（同步、无真实 I/O）。真实 SQLite/ffi 覆盖在 `test/repository_test.dart`、`test/controller_persistence_test.dart`、`test/migration_matrix_test.dart`、`test/model_roundtrip_test.dart` 与 `test/repository_contract_test.dart`。测试覆盖用户可见行为（渲染、交互、导航、表单校验）。修改功能后至少运行 `flutter analyze` 和 `flutter test`。

### 技术决策

关键技术选型与理由记录在 [docs/dev/tech-decisions.md](docs/dev/tech-decisions.md)，变更相关实现时同步更新。功能演进的待办不在仓库内维护。

## 关键约束

- **Android 功能必须有真实实现**：涉及本地存储、图片、文件、安装包或系统入口的功能，必须确认 Android 端有真实实现，并检查权限（Manifest、分区存储、Android 13+）、持久化和进程重启后的行为。data URL 图片预览用内存图片渲染，不要用 `Image.network`。
- **数据结构变更要同步**：新增本地数据项时，确认导出/导入、设置页初始化、进程重启读取都覆盖到，并更新样例备份 `docs/dev/verifin-sample-backup.json`（测试应能真实导入它）。
- **文档同步**：变更影响开发命令、架构、交付流程、配置或用户可见行为时，须在同一次变更中更新 `README.md`、`AGENTS.md`、`docs/`。
- **CHANGELOG 同步**：凡用户可见的改动（新功能、界面/交互变化、修复、隐私安全项等），在同一次变更中往 `CHANGELOG.md` 的 `## [Unreleased]` 段落加一行（分类到 新增/优化/修复/安全 等小节）；纯内部重构、改测试/文档等对用户无感的不写。发版时再把 `Unreleased` 提升为版本号（见「发布与 CI」）。
- UI 风格：紧凑型移动端工具风格；主色 `#346edb`，辅助蓝 `#3498db`，青绿色仅用于收入/正向状态。UI 规范见 `docs/ui-guidelines.md`。
- **日期算术禁用裸 `Duration`**：「相隔几天」走 `calendarDaysBetween`、「推 N 天」走 `addCalendarDays`（[lib/app/calendar_days.dart](lib/app/calendar_days.dart)，经 `ledger_math.dart` re-export），**不要写 `difference().inDays` / `add(Duration(days: n))`**——那是绝对时间，夏令时地区一天可能 23/25 小时，会少算一天或让时刻漂移一小时（历史 bug：自定义范围天数少 1、每日提醒 21:00 顺延后变 22:00、切换日次日把昨天的交易标成「今天」）。**这类缺陷在 UTC 与中国时区都不显现，CI 恒绿**，只在欧美时区暴露，故必须靠约定而非测试兜住。纯时间间隔（2 秒内双击返回、每隔 N 小时备份）仍用 `Duration`。
- **组件化优先**：写任何 widget/弹窗/对话框/格式化/计算前，先查 `docs/dev/components.md` 组件清单，命中就复用或参数化扩展，不新建变体。2–8 项静态受控单选走 `VeriAnchoredChoice<T>`，动态/分区长列表走 `showOptionSheet` 或领域 Picker；图标渲染走 `CategoryIconBox`/`AccountIconBox`（勿直接 `iconForCode`），弹窗走 `show*Sheet`/`showConfirmDialog`/`showTextInputDialog`（勿裸包 `showModalBottomSheet`/内联 `AlertDialog`）。完整代码规范见 `AGENTS.md` 的「代码规范 · 组件化与工程约定」。已知架构限制与技术债见 `docs/dev/known-limitations.md`。
- 不要重写生成的平台工程文件（`android/`），除非任务明确要求；不要为简单需求引入额外工具链或依赖。
